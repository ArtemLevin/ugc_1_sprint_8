<yandex>
    <users>
        <admin>
            <password>admin</password>
            <networks>
                <ip>::/0</ip> <!-- доступ с любых IP -->
            </networks>
            <profile>default</profile>
            <quota>default</quota>
        </admin>

        <!-- default пользователь с пустым паролем -->
        <default>
            <password></password>
            <networks>
                <ip>::/0</ip>
            </networks>
            <profile>default</profile>
            <quota>default</quota>
        </default>
    </users>
</yandex>
